import 'package:flutter/material.dart';

class Grid extends Padding {
  const Grid({
    super.key,
  }) : super(
          padding: const EdgeInsets.only(
            left: 16,
          ),
        );
}
